# notas

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base de la tabla de mutiplicar        [number] [required]
  -l, --list     muestra la tabla en consola                           [boolean]
  -h, --hasta    pone limite a la tabla                                 [number]
```